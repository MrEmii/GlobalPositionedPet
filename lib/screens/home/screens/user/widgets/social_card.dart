import 'package:flutter/material.dart';

class SocialCard extends StatelessWidget {

  final String userid;
  final String postid;

  SocialCard({this.userid, this.postid});//TODO: Fetch a backend para obtener post

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}